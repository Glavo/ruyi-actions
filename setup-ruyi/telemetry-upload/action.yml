name: Ruyi Telemetry Upload
description: Register a post-run telemetry upload for setup-ruyi.

inputs:
  ruyi-bin:
    description: Absolute path to the ruyi executable.
    required: true

runs:
  using: node24
  main: index.js
  post: index.js
  post-if: always()
