# Meson RISC-V Hello World

This example is a minimal Meson project used to test `setup-ruyi` and `setup-ruyi-venv`.

The demo workflow creates a Ruyi virtual environment, configures Meson with the generated cross file, and builds this program for RISC-V.
