# CMake RISC-V Hello World

This example is a minimal CMake project used to test `setup-ruyi` and `setup-ruyi-venv`.

The demo workflow creates a Ruyi virtual environment, configures CMake with the generated toolchain file, and builds this program for RISC-V.
